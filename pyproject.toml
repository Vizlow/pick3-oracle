[project]
name = "pick3-oracle"
version = "1.0.0"
description = "NY Pick 3 prediction + honest tracking system"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py39"
